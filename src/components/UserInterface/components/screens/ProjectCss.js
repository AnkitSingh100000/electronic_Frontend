import { makeStyles } from "@mui/styles";
const useStyles = makeStyles({
    root: {
      background: '#191919',
      height: "100%",
      width:'100%',
    },
    Box: {
      background: '#f1f2f6',
      height: "100vh",
      width:"100%",
    },
  });
  export {useStyles}